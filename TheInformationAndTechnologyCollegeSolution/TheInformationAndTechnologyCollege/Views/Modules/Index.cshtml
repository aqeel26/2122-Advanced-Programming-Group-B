@model IEnumerable<TheInformationAndTechnologyCollege.Models.Module>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ModuleCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModulTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModuleDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModuleContents)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ModuleCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModulTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModuleDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModuleContents)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ModuleID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ModuleID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ModuleID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
